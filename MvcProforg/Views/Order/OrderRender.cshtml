@model IEnumerable<ProforgDataEF.tab_order>

@{
    ViewBag.Title = "Замовлення";
}

<h2>Замовлення</h2>

<p>
    @Html.ActionLink("Додати до замовлення", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.order_count)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tab_students.student_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.order_count)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tab_students.student_name)
        </td>
        <td>
            @Html.ActionLink("Редагувати", "Edit", new { id=item.order_ID }) |
            @Html.ActionLink("Видалити", "Delete", new { id=item.order_ID })
        </td>
    </tr>
}

</table>
